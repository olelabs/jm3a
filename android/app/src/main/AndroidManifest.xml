<manifest xmlns:android="http://schemas.android.com/apk/res/android">
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
   <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
   <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
   <uses-permission android:name="android.permission.VIBRATE"/>
   <uses-permission android:name="android.permission.RECORD_AUDIO"/>
   <!-- Item 7 (QR codes) — mobile_scanner needs this to open the camera
        for scanning a room/profile QR code. -->
   <uses-permission android:name="android.permission.CAMERA"/>
   <uses-feature android:name="android.hardware.camera" android:required="false"/>
   <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>
    <application
        android:label="jma3a"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <receiver android:name="me.carda.awesome_notifications.core.broadcasters.receivers.ScheduledNotificationReceiver"/>
        <receiver android:name="me.carda.awesome_notifications.core.broadcasters.receivers.NotificationDismissedReceiver"/>
        <receiver android:name="me.carda.awesome_notifications.core.broadcasters.receivers.RefreshSchedulesReceiver" android:exported="true">
            <intent-filter>
            <action android:name="android.intent.action.BOOT_COMPLETED"/>
            <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
            <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Custom-scheme deep link — internal-routing fallback only
                 (QR codes, other in-app surfaces). Never shared externally:
                 a jma3a:// link renders as plain, non-tappable text in
                 WhatsApp/SMS/etc. autoVerify is meaningless for a non-http(s)
                 scheme, so it's omitted here (see the App Links filter below
                 for the one that actually needs it). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- jma3a://join?code=ABC123&invited_by=uuid -->
                <data android:scheme="jma3a" />
            </intent-filter>

            <!-- Android App Links — what every SHARED link actually uses:
                 https://jma3a.com/join?code=ABC123&invited_by=uuid,
                 https://jma3a.com/profile/<uuid> (legacy profile links —
                 see deep_links.dart's parseProfileLink), and
                 https://www.moujgroup.jma3a.com/u/<username> (AppConstants.
                 publicProfileUrl — the current, UUID-free profile share
                 link; a separate host from jma3a.com per product decision
                 — see AppConstants.profileWebHost). The legacy
                 https://jma3a.com/u/<username> host is also kept here so a
                 profile link shared before that host switch still opens
                 the app. /profile and /u were previously missing from this
                 filter entirely — a shared profile link only ever opened
                 in the browser on Android, never the app; adding them
                 here is what actually makes the deep link work, not just
                 look right. autoVerify="true" makes Android verify
                 ownership of EACH host against its own
                 /.well-known/assetlinks.json (must be hosted there — see
                 assetlinks.json.template in this repo; www.moujgroup.
                 jma3a.com needs its own copy, which this repo cannot
                 verify is deployed) before it'll open the app directly
                 instead of a browser tab; until that file is live and
                 verification succeeds for a given host, Android silently
                 falls back to opening the URL in the browser, landing on
                 the website instead — which is exactly the "app not
                 installed" fallback requirement, so no further code needs
                 to handle that case explicitly. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="jma3a.com"
                    android:pathPrefix="/join" />
                <data
                    android:scheme="https"
                    android:host="jma3a.com"
                    android:pathPrefix="/profile" />
                <data
                    android:scheme="https"
                    android:host="jma3a.com"
                    android:pathPrefix="/u" />
                <data
                    android:scheme="https"
                    android:host="www.moujgroup.jma3a.com"
                    android:pathPrefix="/u" />
            </intent-filter>
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
